import 'package:flutter/material.dart';

class HistoScreen extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mapas change'),
    );
  }
}
